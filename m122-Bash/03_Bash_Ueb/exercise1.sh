#!/usr/bin/bash

# Exercise 1 - Repetition: Navigation in directiories
# ---
# 1. Change in the home directiory using cd:
cd ~
# ---
# 2. Change into the /var/log directory using an absolute path:
cd /var/log
# ---
# 3. Change into the /etc/udev directory using an absolute path:
cd /etc/udev
# ---
# 4. Change into the /etc/ directory using a relative path:
cd ..
# ---
# 5. Change into the /etc/network/ directory using a relative path
cd network
# ---
# 6. Change into the /dev/ directory using a relative path
cd ../../dev
# ---