apiVersion: apps/v1
kind: Deployment
metadata:
    name: sendreceive-deployment
    namespace: kn08
spec:
    replicas: 1
    selector:
        matchLabels:
            app: sendreceive
    template:
        metadata:
            labels:
                app: sendreceive
        spec:
            containers:
                - name: sendreceive
                  image: sxnic/kn08-sendreceive:v2
                  ports:
                    - containerPort: 8003
                  env:
                      - name: PORT
                        valueFrom:
                            configMapRef:
                                name: sendreceive-env
                                key: port
                      - name: ACCOUNT_SERVICE_URL
                        valueFrom:
                            configMapRef:
                                name: account-env
                                key: url
---
apiVersion: v1
kind: Service
metadata:
    name: sendreceive-service
    namespace: kn08
spec:
    selector:
        app: sendreceive
    ports:
        - protocol: TCP
          port: 8003
          targetPort: 8003